apiVersion: v1
kind: Secret
metadata:
  name: todo-secret
  namespace: todo-app
type: Opaque
stringData:
  DB_PASSWORD: "postgres"  # Change this to your actual password
  # To create secret from command line:
  # kubectl create secret generic todo-secret --from-literal=DB_PASSWORD=yourpassword -n todo-app

